<%provide(:title, "All Posts") %>
<% @posts.each do |post| %>
  <div class="post_wrapper">
    <h2 class="title">
      <%= link_to post.title, post %>
    </h2>
    <p class="date">
      <%= post.created_at.strftime("Posted on %B, %d, %Y, at %I:%M %p %Z")%>
    </p>

  </div>
<% end %>
